# Phase 7 — Player visual vertical slice

The oath knight uses original GPT Image artwork generated for ASHEN VOW, with separate transparent upper-body, left-leg and right-leg regions in `assets/characters/oath_knight.png`. The generated sheet remains the editable source asset. `actors/player.gd` crops the three parts at runtime, retaining the previous atlas architecture and its independent footstep motion. The old SVG stays available as a source reference.

At normal play scale the broken crown, oxidized teal cloak, eye slit and bright antique metal distinguish the player against cathedral floors. Motion uses a 0.20-second visual strike window already established by `CombatChain`; no attack cooldown, hit window or damage rule changes. Sword swings compactly, scythe rotates, spear thrusts far, staff projects straight, fists jab rapidly, mace swings heavily and spellblade combines an arc with forward extension. Dash clears attack motion immediately. Damage flashes, and fatal damage draws a collapsed pose on the final frame.

Production source: original image generation guided by `references/art-direction.md` and `references/audio-motion.md`; two generated references were used, with the separated atlas committed as the runtime source. No third-party art. No BGM/SFX replacement belongs to this player-visual Phase; the existing sound events remain synchronized to the original gameplay actions.

Validation: Godot 4.5.1 imports the PNG and scripts; `phase7_runner.gd` checks alpha cutouts, seven real attacks with dodge cancel, and repeated three-weapon order. `phase6_runner.gd` remains green. The normal full campaign reaches victory. `phase7_visual.gd` captures 15 desktop-rendered images at 1180×737 under the iPad landscape touch layout, including all seven strikes, an actual three-weapon chain, movement, dodge, hit and collapse. Rendered screenshots were inspected for body/leg assembly, weapon positions and contrast. Local Web export succeeds; an HTTP server serves the HTML, PCK, WASM, manifest and service worker, and the manifest specifies landscape standalone PWA. PR #44 CI passed Visual smoke, AI asset validation and Web build with Chromium browser runtime smoke. Physical iPad Safari was not exercised; local Chromium download was unavailable in this environment.
